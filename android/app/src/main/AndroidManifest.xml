<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network for the encrypted relay + OSM tiles -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Foreground location for live party position sharing -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Background location so the crew can see each other when the phone is
         in a pocket. Requires a prominent in-app disclosure before the OS
         prompt (see lib/features/onboarding/screens/background_disclosure_screen.dart)
         and a Background Location Use justification on the Play Console
         submission (see docs/play-background-location-submission.md). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <!-- Foreground service for background location updates (Android 10+).
         FOREGROUND_SERVICE_LOCATION is the granular subtype required on
         Android 14+; geolocator's foregroundNotificationConfig creates the
         service with this type automatically when configured. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Wake the device to deliver a fresh fix while the screen is off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Dead-man's-switch / crew alerts -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Full-screen alert when a party member triggers Man-down. The OS
         allows the FCM payload's high priority + this permission to wake
         the screen and present an interstitial; without it Android shows
         a normal banner that's silent in DND mode. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Camera for pin photos (image_picker) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- BLE — Meshtastic radio integration. On Android 12+ (API 31) BLUETOOTH_*
         replace the legacy ACCESS_BLUETOOTH; we declare both so the app works
         from Android 6 onward. usesPermissionFlags="neverForLocation" tells
         Android we will not infer user location from BT scan results, which
         lets us scan without ACCESS_FINE_LOCATION on 12+ (we already have it
         for GPS anyway). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false"/>
    <application
        android:label="innawoods"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
